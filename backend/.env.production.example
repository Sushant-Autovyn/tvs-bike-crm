# Production Environment Variables for Render Deployment
# Copy these values to your Render Dashboard → Environment Variables

NODE_ENV=production
PORT=10000

# MongoDB Atlas Connection (REQUIRED)
MONGO_URI=mongodb+srv://sushantgodara439_db_user:dNspe8iSD90eTJJC@cluster0.nbknz28.mongodb.net/bikecrm_db?retryWrites=true&w=majority&appName=BikecrM

# Security
JWT_SECRET=your_super_secret_key_production_change_this

# CORS (Update with your actual frontend domain)
CORS_ORIGIN=https://your-frontend-app.onrender.com

# Optional: Additional Atlas settings
DB_NAME=bikecrm_db

# Render Deployment Notes:
# 1. Set NODE_ENV=production in Render environment variables
# 2. Ensure MONGO_URI points to your active Atlas cluster
# 3. Update JWT_SECRET with a strong production secret
# 4. Update CORS_ORIGIN with your actual frontend URL
# 5. Make sure Atlas cluster is not paused
# 6. Verify Network Access allows Render's IPs (or use 0.0.0.0/0)